{{!< default}}
{{!--
  Generic micro-stream fallback. The per-language micro streams are served by the
  wrapper templates (micro-<code>.hbs), which inject langtag="hash-<code>"; this bare
  include renders the same body with no language binding. See partials/micro-stream.hbs.
--}}
{{> "micro-stream"}}
