{{!< default}}
{{!--
  Wrapper template for the /us/micro/ stream. Injects the language's internal
  tag slug (hash-us) into the shared body so the micro stream and its infinite
  scroll stay scoped to this language. See partials/micro-stream.hbs.
--}}
{{> "micro-stream" lang="hash-us"}}
