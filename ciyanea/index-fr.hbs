{{!< default}}
{{!--
  Wrapper template for the /fr/ language homepage. Injects the language's
  internal tag slug (hash-fr) into the shared body so language-scoped Content
  API queries (hero, sidebar micro-preview) resolve to this language. See
  partials/home.hbs for the actual layout.
--}}
{{> "home" lang="hash-fr"}}
