{{!< default}}
{{!--
  Wrapper template for the /es/ language homepage. Injects the language's
  internal tag slug (hash-es) into the shared body so language-scoped Content
  API queries (hero, sidebar micro-preview) resolve to this language. See
  partials/home.hbs for the actual layout.
--}}
{{> "home" langtag="hash-es"}}
