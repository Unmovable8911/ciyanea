{{!< default}}
{{!--
  Generic homepage fallback (Ghost requires index.hbs to exist). The language
  homepages are served by the per-language wrapper templates (index-<code>.hbs),
  which inject lang="hash-<code>"; this bare include renders the same body with no
  language binding for any non-prefixed/unmatched collection. See partials/home.hbs.
--}}
{{> "home"}}
