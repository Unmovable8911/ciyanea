{{!< default}}
{{!--
  Wrapper template for the /cn/micro/ stream. Injects the language's internal
  tag slug (hash-cn) into the shared body so the micro stream and its infinite
  scroll stay scoped to this language. See partials/micro-stream.hbs.
--}}
{{> "micro-stream" lang="hash-cn"}}
