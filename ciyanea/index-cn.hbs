{{!< default}}
{{!--
  Wrapper template for the /cn/ language homepage. Injects the language's
  internal tag slug (hash-cn) into the shared body so language-scoped Content
  API queries (hero, sidebar micro-preview) resolve to this language. See
  partials/home.hbs for the actual layout.
--}}
{{> "home" langtag="hash-cn"}}
